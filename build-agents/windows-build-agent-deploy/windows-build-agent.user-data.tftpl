${hostname}
${spawn}
${extra_tags}

>>> git-cache-https
${git_cache_https_cert}

>>> vcpkg-cache-https
${vcpkg_cache_https_cert}

>>> ccache-cache-https
${ccache_cache_https_cert}

>>> ccache-cache-client-cert
${ccache_cache_client_cert}

>>> ccache-cache-client-key
${ccache_cache_client_key}
